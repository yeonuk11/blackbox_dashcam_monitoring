# Stage 2

담당 범위: 충돌시점, 차선 진입시점·방향, 회피 공간 분석.

- 구현 진입점: `predict.py`의 `predict(data_dir, model_dir)`
- 출력 열: `ID`, `collision_frame`, `entry_frame`, `evasion_space`, `entry_side`
- 로컬 가중치 기본 위치: `best.pt` (`.gitignore` 대상)

현재 코드는 공용 skeleton 검사용 placeholder입니다.
